shell=bash
disable=SC2034  # unused vars
disable=SC2154  # unassigned: $srcdir/$pkgdir/$CARCH injected by makepkg at runtime
disable=SC2164  # cd without || exit: functions run under `bash -e` (PKGBUILD(5))
